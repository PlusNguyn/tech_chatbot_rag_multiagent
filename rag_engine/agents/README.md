# Agents

LangGraph workflow package for the product-consulting RAG chatbot.

The graph currently uses five focused agents: `supervisor`, `retrieval`, `reranker`, `advisor`, and `guardrails`. Each agent folder owns one graph node and documents its responsibility in a local `README.md`.

